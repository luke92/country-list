# country-list
JSON Files of Country with states

## How can use
- You can use json files for load translations or
- You can load the json language in Console JavaScript and use the function `reloadCountriesWithJson(json)`;
- The JSON must have the format of json files from this repository